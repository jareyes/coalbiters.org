const config = require("config");
const crypto = require("node:crypto");
const {Router} = require("express");
const User = require("../lib/model/user");

function login(req, res, next, sqlite) {
    try {
        const 
    }
    catch(err) {
        next(err);
    }
}
